# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 明烛运行时数据（不入库）
# 注意：cost_monitor.py 实际写到仓库根目录的 cost_log.json，
#       memory.py 写到 memory_store/，evolution 写到 evolution/
agent_system/memory_store/
memory_store/
/cost_log.json
agent_system/evolution/
evolution/

# 敏感配置（含 API Key、出生信息等，绝不入库）
.env
.env.local
.env.*.local

# 注意：master_profile/ 含命理与档案数据，作为项目核心数据保留入库。
#       若仓库转为公开，应将 master_profile/ 加入忽略并迁移到 .env。

# IDE
.vscode/
.idea/
*.swp
knowledge_base/
chroma_db/
